# VibedInsight Configuration
# Copy this file to .env and adjust values

# Domain (same as in /srv/ai-lab/.env)
DOMAIN=your-domain.com

# Timezone
TZ=Europe/Berlin

# PostgreSQL
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Ollama Model (optional, default: llama3.2)
OLLAMA_MODEL=llama3.2

# ============================================================================
# JWT Authentication (REQUIRED for production!)
# ============================================================================

# Secret key for signing JWT tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANT: Keep this secret! If compromised, regenerate immediately.
JWT_SECRET_KEY=CHANGE_ME_TO_RANDOM_SECRET_KEY

# Token expiry times (optional, defaults shown)
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
# JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Minimum password length (optional, default: 8)
# MIN_PASSWORD_LENGTH=8
